# Phase 1 — Scaffold

This branch contains the initial scaffold for Phase 1: Next.js (App Router) + TypeScript + Tailwind CSS.

Quick start:

```bash
npm install
npm run dev
```

Notes:
- Run `npm run typecheck` to validate TypeScript configuration.
